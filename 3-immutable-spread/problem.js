/**
 * 1. How can we create immutable primitive values like a boolean, a number, or a string?
 */

var aBoolean = true;
var aString = 'something';
var aNumber = 2;
















// ------------------------------------------------------------------

/**
 * 2. How can we create immutable objects?
 */

let pokemon = { name: 'mew', foundIn: 'Kanto' };

// ==> Can we change the name?













// ------------------------------------------------------------------

/**
 * 3. If we cannot change immutable objects, how do we update their properties?
 */













// ------------------------------------------------------------------

/**
 * 4. What happens when we use the spread operator on arrays?
 */












// ------------------------------------------------------------------

/**
 * 5. Curiosity: Try out spreading a string and printing it
 * What do you get?
 */